module.exports = {
	plugins: [
		require('autoprefixer')/*,
		TODO: try again in a while. Currently, this failed stating
		"PostCSS plugin postcss-focus requires PostCSS 8"
		but I was running PostCSS 8 at the time
		require('postcss-focus')
		*/
	]
}
