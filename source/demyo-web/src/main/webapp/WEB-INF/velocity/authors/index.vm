#setPageTitle('title.index.author')

#fab('./add', 'add')

#textCardListStdBody($authorList "name" "identifyingName" "./view/" true)

$paging.pageLinks($authorList, $request, $javascript)
$paging.letters($request)
